# Supabase Auth (GoTrue) for Axum

_Use Supabase Auth (GoTrue) with PKCE flow as an external authentication library for your Axum + Rust webservices_

## Usage

```bash
cargo add axum-supabase-auth
```

```rust

```